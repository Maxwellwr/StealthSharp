name: Stealth

on:
  release:
    types: [published]

jobs:
  build:

    runs-on: ubuntu-latest
    env:
      NUPKG_FOLDER: nupkg
      PROJECT_FOLDER: src
      NUGET_APIKEY: ${{ secrets.NUGET_TOKEN }}
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: dotnet restore $PROJECT_FOLDER
    - name: Build
      run: dotnet build -p:Version=$GITHUB_REF -c Release --no-restore $PROJECT_FOLDER
    - name: Test
      run: dotnet test --no-build --verbosity normal -c Release --filter Category=Unit $PROJECT_FOLDER
    - name: Pack
      run: dotnet pack -p:Version=$GITHUB_REF -p:PackageOutputPath=../$NUPKG_FOLDER -c Release --no-build --nologo $PROJECT_FOLDER
    - name: Deploy
      run: dotnet nuget push "${PROJECT_FOLDER}/${NUPKG_FOLDER}/*.${GITHUB_REF}.nupkg" -k ${NUGET_APIKEY} 

